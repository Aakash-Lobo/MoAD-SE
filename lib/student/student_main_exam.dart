import 'package:flutter/material.dart';

class MainExam extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      debugShowCheckedModeBanner: false,
      // home: homepage(),
    );
  }
}
